import { Box, Typography } from '@mui/material'

function Footer(){

    return (
        <Box sx={{ mt: 'auto' }}>
            <Typography color='inherit' my={4} textAlign='center'>Some footer text</Typography>
        </Box>
    )

} export default Footer